# Server
PORT=3001
RUST_LOG=info

# Data source: "local" or "s3"
DATA_SOURCE=local

# Local filesystem (when DATA_SOURCE=local)
DATA_PATH=./output

# S3 (when DATA_SOURCE=s3)
AWS_REGION=ap-southeast-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
S3_BUCKET=
S3_PREFIX=output
